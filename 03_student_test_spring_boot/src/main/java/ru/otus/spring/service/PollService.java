package ru.otus.spring.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import ru.otus.spring.service.interfaces.IOService;

//@RequiredArgsConstructor
//@Service
// Just for testing. I don't use it yet. TODO: Delete this later.
public class PollService {

    /*private final IOService ioService;

    public void poll() {
        ioService.out("What's your name?");
        String name = ioService.readString();
        ioService.out(String.format("Приятно познакомиться %s", name));
        ioService.out("Как ваши дела?");
        String res = ioService.readString();
        if (res.equals("OK")) {
            ioService.out("Это радует");
        } else {
            ioService.out("Ничего, скоро все поправится");
        }
    }*/

}
